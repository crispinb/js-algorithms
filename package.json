{
  "name": "wengrow_js",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "//": "haven't been able to figure out how to do htis from powershell so just set with $env:NODE_OPTIONS='--experimental-vm-modules' in console before running npx jest ..",
    "test": "NODE_OPTIONS=--experimental-vm-modules jest",
    "//": "npm run dev for iterating on a new function without running the perf tests",
    "dev": "NODE_OPTIONS=--experimental-vm-modules jest --watch -t dev"
  },
  "jest": {
    "//": "no-transforms allows jest tests to use es imports",
    "transform": {}
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/jest": "^27.4.0",
    "benny": "^3.7.1",
    "jest": "^27.4.7"
  }
}
